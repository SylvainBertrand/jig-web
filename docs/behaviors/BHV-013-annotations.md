# BHV-013: Annotations

Click Annotate (flag icon) -> popover with text input + color
Type label, press Enter -> colored marker on scrub bar
Hover marker -> tooltip shows label
Markers also visible as vertical lines on chart
