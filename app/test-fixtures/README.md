# Test Fixtures

Place real .mcap files here for testing.

The user will provide a real-world .mcap file. The QA agent (Wave 8) uses
this file to verify that MCAP loading works with real data, not just
synthetic demo data.

If no .mcap fixture is present, Wave 8 QA will test with demo mode only.
